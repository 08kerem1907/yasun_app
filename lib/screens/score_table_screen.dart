
import 'package:flutter/material.dart';

class ScoreTableScreen extends StatelessWidget {
  const ScoreTableScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Puan Tablosu Ekranı'),
      ),
    );
  }
}

